# PRODUCTION ENVIRONMENT - Frontend
# Copy this to .env.local for production

# Backend URL - should point to your production domain with proper SSL
NEXT_PUBLIC_BACKEND_URL=https://YOUR_DOMAIN.com

# DO NOT set NODE_TLS_REJECT_UNAUTHORIZED=0 in production!
# Use proper SSL certificates instead
