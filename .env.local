# Backend URL - connects to nginx which proxies to the Rust backend
NEXT_PUBLIC_BACKEND_URL=https://127.0.0.1

# Default school tenant for development (no subdomain needed)
NEXT_PUBLIC_DEFAULT_SCHOOL=stellamaris

# Node.js TLS settings for self-signed certificates
NODE_TLS_REJECT_UNAUTHORIZED=0
